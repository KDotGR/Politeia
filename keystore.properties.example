# Copy this file to keystore.properties and replace every value before producing a signed bundle.
# Never commit the copied file or the keystore. Both are ignored by .gitignore.
storeFile=/absolute/path/to/politeia-upload.jks
storePassword=REPLACE_WITH_STORE_PASSWORD
keyAlias=politeia-upload
keyPassword=REPLACE_WITH_KEY_PASSWORD
